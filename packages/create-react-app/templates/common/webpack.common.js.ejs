module.exports = {
  entry: {
    main: "./src/index.js",
  },

  resolve: {
    extensions: [".tsx", ".ts", ".jsx", ".js", ".json"],
  },

  module: {
    rules: [
      {
        test: /\.(ts|tsx|js|jsx)$/,
        exclude: /node_modules/,
        use: {
          <%_ if (options.transpiler === "swc") { -%>
          loader: "swc-loader",
          <%_ } else { -%>
          loader: "babel-loader",
          <%_ } -%>
        },
      },
      {
        test: /\.(gif|jpe?g|png|svg)$/,
        type: "asset/resource",
      },
    ],
  },
};
