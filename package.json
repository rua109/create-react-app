{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "lerna run prepare",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.3",
    "lerna": "^8.0.2"
  }
}
